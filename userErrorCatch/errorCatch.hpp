#include <iostream>
#include <cassert>

class ErrorCatch {
private:

    static std::string checkForRequiredSize(std::string &testStr) {
        ;
    }

    static std::string checkForRequiredSize

};